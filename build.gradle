buildscript {
    repositories {
        jcenter()
        maven {
            name = "forge"
            url = "https://files.minecraftforge.net/maven"
        }
    }
    dependencies {
        classpath "net.minecraftforge.gradle:ForgeGradle:2.3-SNAPSHOT"
    }
}

apply plugin: "net.minecraftforge.gradle.forge"

group = 'ru.starshine-project'
version = project.modVersion
archivesBaseName = 'galacticraftbiology'

minecraft {
    version = project.forgeVersion
    runDir = "run"
    mappings = project.mcpVersion
}

repositories {
    maven {
        url "https://www.cursemaven.com"
    }
}

dependencies {
    //jei
    implementation "curse.maven:jei-238222:$project.jeiVersion"

    //forestry, gt classic, gtc expansion
    implementation "curse.maven:forestry-59751:$project.forestryVersion"
    implementation "curse.maven:gtclassic-324021:$project.gtcVersion"
    implementation "curse.maven:gtсexpansion-349126:$project.gtcExVersion"

    //Galacticraft
    //implementation files('libs/Galacticraft-Planets-Dev-1.12.2-4.0.2.261.jar')
    //implementation files('libs/GalacticraftCore-Dev-1.12.2-4.0.2.261.jar')
    //implementation files('libs/MicdoodleCore-Dev-1.12.2-4.0.2.261.jar')
}

processResources {
    inputs.property "version", project.version
    inputs.property "mcversion", project.minecraft.version

    from(sourceSets.main.resources.srcDirs) {
        include "mcmod.info"

        expand "version": project.version, "mcversion": project.minecraft.version
    }

    from(sourceSets.main.resources.srcDirs) {
        exclude "mcmod.info"
    }
}
